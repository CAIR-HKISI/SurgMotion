# 在 V-JEPA2 核心之上，运行 foundation_phase_probing（含 Bleeding 批量脚本中的各类 backbone，不含 EndoMamba）
-r requirements-vjepa2.txt

# 数据与指标
pandas
scipy
scikit-learn
h5py

# HuggingFace / DINOv3 / VideoMAE 等
transformers>=4.36
accelerate>=0.26
peft
huggingface_hub

# 配置与工具（GastroNet/SurgVISTA 等会用到 defaults / 工具链）
fvcore
omegaconf
iopath
beartype
easydict
regex
tqdm
mmengine==0.7.0

# 日志与实验（eval 中可选；WANDB_MODE=offline 可减少网络依赖）
wandb
tensorboard

# 分布式/集群（evals.main_distributed / SLURM 场景）
submitit

# 其它脚本/库中偶发使用
fire
python-box
ftfy
braceexpand
packaging
